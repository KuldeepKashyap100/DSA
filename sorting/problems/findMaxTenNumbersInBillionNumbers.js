/**
 * Maintain a min priorityQueue and iterated billion number and whenever encounter
 * a number greater than smallest number in queue insert it queue and delete smallest
 * number.
 * 
 * time -> O(billion log(10))
 */